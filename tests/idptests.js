/*jslint
  white: true, vars: true, sloppy: true, devel: true, plusplus: true, browser: true
 */

/*global QUnit, IdentityProvider*/


QUnit.test( "ID Provider Tests", function( assert ) {
    var idp = new IdentityProvider();
    assert.ok(idp);

    /*
     * RSD Loading Tests
     */

});